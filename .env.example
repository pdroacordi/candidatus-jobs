# PostgreSQL connection string
# On Railway: set this to the value from your Railway PostgreSQL add-on
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/candidatus_jobs

# Shared secret for X-API-Key header — change this to a strong random value
API_KEY=change-me-to-a-strong-secret
